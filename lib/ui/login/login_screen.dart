import 'package:flutter/material.dart';

class LogScreen extends StatelessWidget {
  static const String routName="logScreen";

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
